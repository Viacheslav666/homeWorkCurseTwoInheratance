public class Bus extends Transport{
    public Bus(String brand, String model, Integer year, String country, String bodyColor, Integer maximumSpeed) {
        super(brand, model, year, country, bodyColor, maximumSpeed);
    }
}
